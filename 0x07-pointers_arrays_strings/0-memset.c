#include "main.h"

/**
 * _memset - main function
 * @s: 1st parameter value
 * @b: @nd parameter value
 * @n: @rd parameter value
 * Return: s
 */

char *_memset(char *s, char b, unsigned int n);
{
	int i;

	i = 0;
	for (n > 0; i++)
	{
		s[i] = b;
		n--;
	}
	return (s);
}
