#include <stdio.h>

/**
 * Main - entry point
 * function the input
 * retuns a string
 * return : Always 0 (success)
 */

int main(void)
{
	puts("\' Programming is like building a multilingual puzzle\n");
	return (0);
}
